count : INTEGER;
sum : INTEGER := 0;

FOR count := 1 TO 10 STEP 1 DO
    sum := sum + count;
END FOR;
// The sum of the first 10 integers is 55
// The loop iterates from 1 to 10, adding each integer to the sum variable
WRITE(sum);